services:
  - type: web
    name: ai-search-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py serve
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    plan: free

