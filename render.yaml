services:
  - type: web
    name: ai-search-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.serving.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    plan: free
